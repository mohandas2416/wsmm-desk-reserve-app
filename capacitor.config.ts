import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'desk-reservation-react-app',
  webDir: 'dist'
};

export default config;
